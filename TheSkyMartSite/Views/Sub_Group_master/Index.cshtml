@model IEnumerable<TheSkyMartSite.Models.Sub_Group_master>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sub_Group_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Group_master.Group_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sub_Group_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Group_master.Group_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Sub_Group_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Sub_Group_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Sub_Group_ID })
        </td>
    </tr>
}

</table>
